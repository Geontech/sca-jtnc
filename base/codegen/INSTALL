To install the SCA-JTNC Code Generators:

    $ python setup.py install

This will automatically install the Code Generators to $SCAHOME.

If you are installing with the 'sudo' command, note that environment variables
are not inherited. You will need to specify the install location manually:

    $ sudo python setup.py install --home=$SCAHOME
